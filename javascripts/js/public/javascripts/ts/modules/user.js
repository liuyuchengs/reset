define(["require", "exports"], function (require, exports) {
    "use strict";
    /**
     * 用户类
     * 去除了部分无用字段
     */
    var User = (function () {
        function User() {
        }
        ;
        return User;
    }());
    return User;
});
