define(["require", "exports", "angular"], function (require, exports, angular) {
    "use strict";
    var app = angular.module("myApp", ['ngRoute']);
    function globalFn($rootScropt, $location) {
    }
    return app;
});
