import IRootScope = require("IRootScope");
import Ajax = require("../modules/Ajax");
import Tool = require("../modules/Tool");
declare function cush($scope: any, $rootScope: IRootScope.rootScope, ToolService: Tool, AjaxService: Ajax): void;
export = cush;
